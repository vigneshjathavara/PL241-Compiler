# Predefined function and procedure test
main
var a, b, c, d;
{
	let a <- 5;
	let b <- 4;
	let a <- a +1;
	let b <- b+1;
	while a > b do
	let b <- b+2;
	let a <- a+1
	od;
	let c <- a +3 ;
	let d <- b+2
}
.
