main
    var foo, too;
    var af;
    function bar(x);
    var par, q;
    {
    	let par <- 3;
      	let q <- 2;
      	let x <- par * q;
		return x
    };
    function foo();
    var par, q;
    {
       let par <- 3;
      	let q <- 2;
      	let q <- par * q;
		return q
    };
    {
        let foo <- 3+too;
        call bar(3);
        let af <- foo+ call foo

    }
.