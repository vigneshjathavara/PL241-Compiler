main
    var foo, too;
    var af;
    function bar(x);
    var par, q;
    {
    	let par <- 3;
      	let q <- 2;
      	let x <- par * q;
		return x
    };
    function foo(x);
    var par, q;
    {
       let par <- 3;
      	let q <- 2;
      	let x <- par * q;
		return x
    };
    {
        let foo <- 3+too;
        let af <- foo+6

    }
.